apiVersion: v1
kind: Service
metadata:
  name: gitops-app-service
  labels:
    app: gitops-app
spec:
  selector:
    app: gitops-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer